" nd (new-day): adds mapping that creates a date line at DONE section, moves back to TODO section and creates "Follow-up on stuff" item
nmap nd 'do[<C-R>=strftime("%b %d")<CR>]<ESC>'toFollow-up stuff<ESC>
